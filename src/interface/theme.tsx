export interface colorProps {
    primary: string;
    secondary: string;
    backgroundColor: string;
    default: string;
    input: string;
    helpText: string;
    lightContainer: string;
  }
  
  export interface themeProps {
    colors: colorProps
  }
  
